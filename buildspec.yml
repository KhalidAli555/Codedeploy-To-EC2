version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install -g npm@9
      - npm install

  build:
    commands:
      - echo "Node.js app ready for deployment"

artifacts:
  files:
    - '**/*'
